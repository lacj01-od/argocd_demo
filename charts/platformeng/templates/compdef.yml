# Code generated by KubeVela templates. DO NOT EDIT. Please edit the original cue file.
apiVersion: core.oam.dev/v1beta1
kind: ComponentDefinition
metadata:
  annotations:
    definition.oam.dev/description: My Sample ODP Appl Type
  name: odpsample
spec:
  schematic:
    cue:
      template: |
        output: {
        	spec: {
        		selector: matchLabels: "app.oam.dev/component": parameter.name
        		template: {
        			metadata: labels: "app.oam.dev/component": parameter.name
        			spec: containers: [{
        				name:  parameter.name
        				image: parameter.image
        			}]
        		}
        	}
        	apiVersion: "apps/v1"
        	kind:       "Deployment"
        }
        parameter: {
        	name:  string
        	image: string
        }
  workload:
    definition:
      apiVersion: apps/v1
      kind: Deployment

