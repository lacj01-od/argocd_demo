apiVersion: core.oam.dev/v1beta1
kind: Application
metadata:
  name: first-vela-app
spec:
  components:
    - name: express-server
      type: odpsample
      properties:
        image: bootstrap:5577/mytstapp:latest
        name: myexpresserver

  policies:
    - name: minikube
      type: topology
      properties:
        clusters: [ "minikube" ]

  workflow:
    steps:
      - name: deploy
        type: deploy-minikube
        properties:
          policies: [ "minikube" ]
